package app;

import core.BoxPuzzle;

/**
 * Main application entry point for the Box Top Side Matching Puzzle Game.
 * This class contains only the main method that starts the game.
 */
public class BoxPuzzleApp {
    /**
     * Main method that starts the Box Puzzle game.
     * Creates a new BoxPuzzle instance which initializes and runs the game.
     * 
     * @param args command line arguments (not used)
     */
    public static void main(String[] args) {
        new BoxPuzzle();
    }
}
